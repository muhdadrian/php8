<?php 

$isComplete = true;//if this is false, the output is blank as it's just an empty string

//another way to print boolean is simply by echoing out the bool
echo $isComplete;

if ($isComplete) {
	//echo 'success';
} else {
	//echo 'fail';
}